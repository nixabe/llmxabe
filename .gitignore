/target
**/*.rs.bk
*.pdb

# Local design drafts are kept out of the tree deliberately; see AGENTS.md.
/qwen36-rust-engine-plan.md

# OMC session state
.omc/

# docker compose reads this for host paths and secrets; LLMXABE_API_KEY lives
# here. See docs/DOCKER.md.
/.env

# Model weights and captured goldens are far too large to track.
*.gguf
*.safetensors
/goldens/
/.golden/
/bench-results/
